你是一名资深全栈技术负责人，擅长制定团队编码规范，关注一致性、可维护性与上下游对齐。请基于 PRD 与开发计划生成一份编码规范，写入 /Users/sunbao/Documents/workspace/trae/用法/project1/docs/编码规范.md。

【执行前置】
- 必须先用 Read 工具读取以下两份文档全文，不得凭上下文臆测：
  1. PRD：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/项目背景.md
  2. 开发计划：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/开发计划.md
- 若发现关键信息（技术栈版本、目录结构、权限模型、删除分级、接口契约、数据库迁移约定）在两份文档间缺失或矛盾，必须先列出澄清问题等我回复，再产出编码规范；在此之前不要直接生成正文
- 与 PRD / 开发计划矛盾的措辞一律以 PRD 为准（如角色数量、P0 范围、鉴权模式）

【输入】
- PRD：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/项目背景.md
- 开发计划：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/开发计划.md
- 技术栈：以 PRD 第 1 章为准（后端 Python+Django；前端 React+TypeScript+Vite；sqlite3，预留 MySQL 迁移）
- 团队配置：前后端共 1 人全职参与 + Trae（AI 辅助编码）
- 输出文件：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/编码规范.md（绝对路径）

【产出要求】用 Markdown 输出，章节顺序与编号保持一致，至少覆盖以下内容：

1. 技术栈
   - 后端：Python 版本（通过 pyenv 管理，项目根目录提供 .python-version）、Django 版本（LTS）、DRF、JWT 库、密码哈希库、sqlite3 + MySQL 迁移预留
   - 前端：Node.js（通过 nvm 管理，项目根目录提供 .nvmrc）、React 18 函数组件、TypeScript strict、Vite、React Router v6、Axios、Tailwind CSS
   - 工具链：
     - 版本管理工具：Python 用 pyenv（.python-version）、Node.js 用 nvm（.nvmrc），用表格呈现
     - 虚拟环境：后端用 venv 隔离依赖
     - 包管理（pip / pnpm）、格式化（black+isort / Prettier）、Lint（ruff / ESLint）、pre-commit 钩子

2. 项目目录结构
   - 后端目录（Django apps 拆分：users / todos / groups / notifications / audit / common，配置按 base/dev/prod 拆分）
   - 前端目录（src/ 下 api / components / pages / hooks / store / types / utils / constants）
   - 目录命名需与开发计划的模块划分（一.1）对齐

3. 命名约定
   - Python / Django：模块/类/函数/变量/常量/Model/字段/表名/URL/配置项
   - TypeScript / React：文件/组件/函数/变量/常量/类型/枚举/CSS 类名/Hook
   - 数据库：表名、字段名、主键、外键、时间字段、布尔字段、索引名
   - 用表格给出约定 + 示例

4. 后端代码风格
   - Django Model 规范（含删除分级字段 is_deleted / deleted_by / deleted_at、索引、Meta.db_table、BigAutoField 主键）
   - Serializer 规范（显式 fields 与 read_only_fields）
   - View 规范（DRF ViewSet、permission_classes、pagination_class、get_queryset）
   - 数据访问层抽象（自定义 Manager，封装可见性判定与跨组聚合，隔离 ORM 专有语法以便 MySQL 迁移）
   - 权限引擎规范（独立模块，visibility(user, todo) / can_operate(user, todo, action)，越权统一 403 不泄露资源存在性）
   - 删除管理规范（逻辑删除幂等返回 200、物理删除二次确认、恢复仅限作者本人删除的）
   - 每条配最小可运行代码示例

5. 前端代码风格
   - 组件规范：仅函数组件 + Hooks、显式 Props 类型、单文件不超过 300 行
   - TypeScript 规范：禁止 any（用 unknown + 类型守卫）、类型集中在 src/types/、与后端 Serializer 字段对齐
   - API 封装：Axios 实例 + 拦截器（注入 JWT、统一 401 处理）、按模块拆分到 src/api/
   - 状态管理：优先 useState/useReducer + Context，必要时 Zustand，不用 Redux
   - 路由规范：路径与后端 URL 对齐、受保护路由用 ProtectedRoute 包裹校验角色
   - 配最小可运行代码示例

6. 数据库规范（MySQL 迁移兼容）
   - Django 字段到 sqlite3 / MySQL 的类型映射表
   - 硬约束：禁用 sqlite 专有类型、外键显式声明并启用 PRAGMA foreign_keys=ON、主键统一 BigAutoField、时间 UTC 存储、不依赖隐式类型转换、数据访问层抽象
   - 索引策略（高频过滤字段、复合索引最左前缀、M:N 中间表 UNIQUE 约束）
   - 迁移流程（makemigrations / migrate --plan）

7. 接口规范
   - REST 路由：统一前缀 /api/v1、资源复数、子动作用连字符（与开发计划一.5 对齐）
   - 统一响应格式：成功 {code, data}、失败 {code, message, detail}
   - HTTP 状态码表（含越权统一 403 不泄露资源存在性、冲突 409）
   - 分页：入参 page / page_size（默认 20，最大 50）、出参 items / total / page / page_size
   - JWT 鉴权：Header 格式、Access ≤2h、Refresh ≤7d、全站统一 JWT 不引入 Cookie/CSRF

8. 安全规范
   - 密码加盐哈希（bcrypt/argon2）禁止明文
   - 连续 5 次登录失败锁定 5 分钟（对齐 PRD F18）
   - 越权统一 403 不泄露资源存在性（对齐 PRD F19 与附录 B.1）
   - 二次确认：物理删除、组解散
   - 配置项通过环境变量注入，不硬编码
   - CORS 仅允许前端域名
   - 禁止拼接原生 SQL，使用 ORM

9. 时间与国际化
   - 后端时间 UTC 存储（USE_TZ=True），对外输出 ISO8601
   - 前端按用户本地时区转换展示
   - 本期仅中文，文案集中在 src/constants/i18n.ts 为后续预留

10. 测试规范
    - 后端：单元测试（权限引擎、删除管理、幂等逻辑）、接口测试（F01-F16 全功能点覆盖 400/401/403/409）、越权测试（附录 B 全场景 100% 拦截，独立用例集）
    - 覆盖率：权限引擎与删除管理 ≥ 80%
    - 前端：组件测试（Vitest + Testing Library 可选）、关键交互（表单校验、二次确认、错误提示）
    - 配最小测试用例示例

11. Git 提交规范
    - 分支：main / dev / feature/T01-xxx（对应 WBS 任务 ID）/ fix/xxx
    - 提交信息格式：<type>(<scope>): <subject>，type 含 feat/fix/docs/style/refactor/test/chore
    - 示例对齐 WBS 任务 ID

12. 反例与禁用项
    - 后端：禁止权限判定散落在 View、禁止 View 直接操作 is_deleted、禁止用 404 隐藏越权、禁止 N+1 查询、禁止硬编码配置
    - 前端：禁止 any、禁止 class 组件、禁止组件内直接调 axios、禁止组件内处理 JWT、禁止内联样式
    - 每条配一句话说明正确做法

13. 正确示例索引（表格：场景 → 参考章节）

14. 与 PRD / 开发计划的对齐
    - 角色 4 级（对齐 PRD 附录 A）
    - 权限引擎可见性 6 步规则（对齐 PRD 附录 A）
    - 删除分级与幂等（对齐 PRD 附录 B.4）
    - 越权统一 403（对齐 PRD F19 与附录 B.1）
    - 时间 UTC + ISO8601（对齐 PRD 5.4）
    - MySQL 迁移预留（对齐 PRD 5.4 与开发计划 R5）
    - 单任务 ≤ 2 人日（对齐开发计划约束）

【约束】
- 不要写与 PRD / 开发计划矛盾的约定；如有差异以 PRD 为准并显式标注
- 代码示例必须可运行（导入路径、类名、字段名与目录结构一致）
- 所有命名约定用表格呈现
- 不要空话套话，每条规则必须可执行、可检查
- 版本管理工具：Python 通过 pyenv 管理（项目根目录提供 .python-version），Node.js 通过 nvm 管理（项目根目录提供 .nvmrc）
- 输出文件路径使用绝对路径
