你是一名技术负责人，擅长把需求拆解为可执行的开发计划，关注单人 + AI 辅助场景下的范围控制与关键路径。请基于 PRD 生成开发计划，写入 /Users/sunbao/Documents/workspace/trae/用法/project1/docs/开发计划.md。

【执行前置】
- 必须先用 Read 工具读取 PRD 全文（路径见下方【输入】），不得凭上下文臆测
- 若发现下列关键信息在 PRD 中缺失或矛盾，必须先列出澄清问题等我回复，再产出开发计划；在此之前不要直接生成开发计划正文：
  1. 团队规模与角色分工
  2. 交付范围（P0/P1/P2 取舍）
  3. 工期与排期约束
  4. 数据模型字段级细节
  5. 接口契约规范
- 角色范围必须严格遵循 PRD：固定 4 类（普通用户 / 组长 / 二级管理员 / 一级管理员），不得引入"超级管理员"等第 5 类角色

【输入】
- PRD：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/项目背景.md
- 技术栈：以 PRD 第 1 章为准（后端 Python+Django；前端 React+TypeScript+Vite；sqlite3，预留 MySQL 迁移）
- 团队配置：前后端共 1 人全职参与 + Trae（AI 辅助编码）；所有角色由同一人承担（设计/后端/前端/测试/部署）
- 排期约束：开发周期非强制，按优先级弹性安排，可在 2 周基础上延长；优先保证 P0 完整交付，P1 按余量推进，P2 不纳入本次排期
- 输出文件：/Users/sunbao/Documents/workspace/trae/用法/project1/docs/开发计划.md（绝对路径）

【产出要求】用 Markdown 输出以下三部分，章节顺序与编号保持一致。

## 一、技术方案概要

1. 模块划分（用 Mermaid 架构图：表现层 / 接口层 / 业务层 / 数据访问层 / 基础设施层，标注权限引擎、删除管理、通知等横切模块）
2. 数据模型设计要点
   - 显式列出需建模的多对多关系：用户-组成员、用户-组长、二级管理员-管辖组
   - 给出核心实体字段清单（不写代码，字段级即可），含 is_deleted / deleted_by / deleted_at 等删除分级字段
   - 时间字段统一 UTC + ISO8601 存储；外键显式声明；禁用 sqlite 专有类型，主键类型保持 MySQL 迁移兼容
3. 权限引擎方案
   - 可见性判定规则：严格 4 级（普通用户 / 组长 / 二级管理员 / 一级管理员），与 PRD 附录 A 权限矩阵对齐，不得扩展第 5 类角色
   - 操作权限判定（查看 / 逻辑删除 / 物理删除 / 修改）独立说明，明确"管理员/组长对他人待办仅查看+逻辑删除，不可修改"
   - 越权拦截统一规则引用 PRD 附录 B.1，不在各接口重复实现
4. 删除分级实现方案
   - 逻辑删除字段与幂等规则（已逻辑删除的再次删除返回 200 + 当前状态）
   - 作者对"被他人逻辑删除待办"的可查看、可改、不可恢复边界
   - 物理删除仅限原作者、需二次确认；作者可直接物理删除未逻辑删除或已逻辑删除的待办
5. 接口契约约定
   - REST 路由规范、统一错误格式 {code, message, detail}、JWT 鉴权与刷新机制（全站 JWT，不引入 Cookie/CSRF）、分页参数约定（默认 20/页，最大 50/页）
6. 关键技术风险（针对 PRD 具体场景，至少覆盖以下 5 项）
   - 多对多关系下组长跨组聚合查询的性能与索引策略
   - 退组后历史待办可见性实时判定的事务一致性
   - 逻辑删除幂等与作者恢复权限边界
   - 单人 + AI 辅助下范围蔓延与质量保障风险
   - sqlite → MySQL 迁移预留的兼容性陷阱

## 二、任务拆解（WBS）

用如下表格，字段不可缺省：

| 任务ID | 任务名 | 模块 | 阶段 | 关联功能点 | 依赖任务 | 预估(人日) | 优先级 | 负责人角色 | 产出物 | 验收标准 |

字段说明：
- 阶段：取值 设计 / 开发 / 联调 / 测试 / 部署
- 关联功能点：对应 PRD 的 F01-F28，确保可追溯；P0 任务必须覆盖 F01-F16 全部 16 项
- 负责人角色：单人全揽，统一填"全栈(单人+Trae)"
- 产出物：如 ER 图 / 接口文档 / 模块代码 / 测试用例 / 部署脚本

WBS 必须显式包含以下非功能任务（不得只拆功能开发）：
1. 环境搭建与脚手架（Django + Vite 工程、配置项环境变量化）
2. 数据库建模与迁移（多对多表、索引、外键显式声明）
3. 权限引擎/可见性判定模块（独立任务，PRD 最难点）
4. 接口契约定义与前后端 Mock
5. 单元测试 / 接口测试 / 权限边界越权测试（覆盖 PRD 附录 B 越权拦截规则）
6. 联调与回归
7. 部署与上线

## 三、开发顺序建议

1. 用 Mermaid gantt 图给出按工作日的单人串行排期（甘特图按天粒度，标注周末）
2. 用 Mermaid flowchart 画出任务依赖关系图，标注关键路径（critical path）
3. 标注里程碑节点，至少含：
   - M1 工程骨架与数据模型就绪
   - M2 P0 后端权限引擎完成
   - M3 P0 前端联调通过
   - M4 P0 全功能验收 + 越权测试通过
   - M5 P1 增强交付（按余量）
   - M6 部署上线
4. 说明：单人场景下"并行"主要体现在"Trae 辅助产出非关键路径产物（如 Mock、测试用例）"，明确哪些任务可借助 AI 并行产出素材、哪些必须人工串行验收

【约束】
- 单个任务不超过 2 人日；超 2 人日的任务必须拆分
- P0 功能点（F01-F16）必须 100% 覆盖到 WBS 任务，不得遗漏
- 权限相关任务必须包含越权测试用例为验收标准之一（覆盖 PRD 附录 B 越权拦截规则）
- 依赖关系必须明确：每个任务标注前置依赖任务 ID，无依赖填"-"
- 不要写代码
- 总工时需与单人配置自洽，并显式给出预计日历周期（如 X 个工作日，可在 2 周基础上延长）
- 输出文件路径使用绝对路径
- 与 PRD 矛盾的措辞一律以 PRD 为准（如角色数量、P0 范围、鉴权模式）
