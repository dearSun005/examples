# Trae 用法指南

> 本指南聚焦于如何通过提示词（Prompt）驱动 Trae 完成软件研发全流程：需求文档 → 开发计划 → 进度跟踪 → 跨会话延续编码。
> 目标：让 AI 成为可信赖的研发搭档，而不是"每次都要重新解释一遍"的黑盒。

---

## 目录

0. [快速上手：跟着做一遍（先看这个）](#0-快速上手跟着做一遍先看这个)
1. [核心心法：结构化提示词四要素](#1-核心心法结构化提示词四要素)
2. [用法一：生成需求文档（含 2.5 复杂场景增强模板）](#2-用法一生成需求文档)
3. [用法二：生成开发计划（含 3.5 复杂场景增强模板）](#3-用法二生成开发计划)
4. [用法三：生成开发进度](#4-用法三生成开发进度)
5. [用法四：跨会话延续编码（关键）](#5-用法四跨会话延续编码关键)
6. [项目级配置文件模板](#6-项目级配置文件模板)
7. [常见反例与改进（含反例 6-8：范围蔓延 / 上下游冲突 / 规则重复）](#7-常见反例与改进)

---

## 0. 快速上手：跟着做一遍（先看这个）

> 这一节不讲理论，只讲**点哪里、粘什么、看什么**。
> 用一个真实场景演示：从零开始做一个"待办事项（Todo）网页应用"。
> 你只需要照着 6 步做，每一步都给出可直接复制的提示词。

### 整体流程图

```
第0步：建3个固定文档（一次性）
        ↓
第1步：让AI写需求文档 → 保存
        ↓
第2步：让AI写开发计划 → 保存
        ↓
第3步：开始第一个任务，让AI写代码
        ↓
第4步：开发完更新进度文档
        ↓
第5步：关掉对话，下次新对话从"读取3个文档"开始继续
```

---

### 第 0 步：建三个固定文档（只做一次）

**做什么**：在项目根目录建一个 `docs` 文件夹，里面放 3 个空文件。

**具体操作**：
1. 打开 Trae，在左侧文件树右键项目根目录 → New Folder → 命名 `docs`
2. 在 `docs` 里右键 → New File，建三个空文件：
   - `项目背景.md`
   - `编码规范.md`
   - `开发进度.md`

**为什么**：这 3 个文件是 AI 的"长期记忆"。新对话每次开头让 AI 读它们，AI 就能接上之前的工作。

**现在它们是空的，没关系，下一步让 AI 帮我们填。**

---

### 第 1 步：让 AI 生成需求文档

**操作**：在 Trae 的对话框里，复制粘贴下面这段话（方框里的全部复制，包括方框）：

```
你是一名资深产品经理。请帮我生成一份需求文档。

【项目背景】
- 项目名：Todo 待办事项应用
- 技术栈：React + TypeScript + Vite
- 当前阶段：从零立项
- 已有模块：无

【目标用户】
个人用户，需要管理日常待办事项。

【核心痛点】
现有的待办应用太复杂，用户只想要一个简单、快速、无广告的工具。

【本次需求目标】
做一个最小可用的 Todo 应用，支持增删改查和完成状态切换。

【产出要求】
生成一份 PRD（Markdown 格式），包含：
1. 背景与目标
2. 功能列表（用表格：功能点 / 优先级 P0-P2 / 描述 / 验收标准）
3. 用户流程（用 Mermaid 流程图）
4. 非功能需求
5. 未纳入本次范围的事项

【约束】
- 不要写代码实现
- 功能点不超过 8 个
- 如信息不足，先列出需要我澄清的问题
```

**看什么**：AI 会输出一份 PRD。检查一下：
- 功能列表是否合理？有没有漏掉你想要的？
- 优先级 P0 是不是真的核心功能？

**保存**：把 AI 输出的内容**全部复制**，粘贴到 `docs/项目背景.md` 的后半部分（前面加一行 `## 需求文档`）。

---

### 第 2 步：让 AI 生成开发计划

**操作**：在**同一个对话**里继续发（不要新开对话，因为 AI 还记得刚才的 PRD）：

```
基于上面这份 PRD，你现在是技术负责人，请生成开发计划。

【输入】
- 上方已生成的 PRD
- 技术栈：React + TypeScript + Vite
- 工期：1 周内完成

【产出要求】
用 Markdown 输出：

1. 技术方案概要
   - 模块划分（用 Mermaid 架构图）
   - 关键技术风险

2. 任务拆解（WBS）
   用表格：
   | 任务ID | 任务名 | 模块 | 依赖任务 | 预估(人日) | 优先级 | 验收标准 |

3. 开发顺序建议
   哪些可以并行，哪些必须按顺序。

【约束】
- 单个任务不超过 2 人日
- 不要写代码
- 依赖关系必须明确
```

**看什么**：检查任务拆解是否合理：
- T001 是不是最基础的（比如项目初始化）
- 依赖关系是否正确（比如"列表页"依赖"数据层"）
- 有没有遗漏（比如忘记类型定义、忘记样式）

**保存**：把这份开发计划复制到 `docs/开发计划.md`，并改造成下面这个格式（这是进度文档，要能跟踪状态）：

```markdown
# 开发进度

## 当前迭代：v0.1.0

## 任务状态

| 任务ID | 任务名 | 状态 | 进度 | 备注 |
|--------|--------|------|------|------|
| T001 | 项目初始化 | ⏳待开始 | 0% | - |
| T002 | 类型定义 | ⏳待开始 | 0% | - |
| T003 | ... | ⏳待开始 | 0% | - |

## 上次中断位置
（还没开始，留空）
```

---

### 第 3 步：开始第一个任务，让 AI 写代码

**操作**：先填好 `编码规范.md`。可以直接用这个最小模板（复制粘贴即可）：

```markdown
# 编码规范

## 技术栈
- TypeScript（strict mode）
- React 18 函数组件
- Tailwind CSS

## 命名约定
- 组件：PascalCase（如 TodoList）
- 函数：camelCase（如 addTodo）
- 常量：UPPER_SNAKE_CASE（如 MAX_TODOS）

## 目录结构
src/
  components/   # 通用组件
  pages/        # 页面
  services/     # API
  types/        # 类型定义

## 代码风格
- 禁止 any
- 函数组件 + 显式 Props 类型

## 正确示例
\`\`\`typescript
interface TodoListProps {
  todos: Todo[];
  onToggle: (id: string) => void;
}
export function TodoList({ todos, onToggle }: TodoListProps) {}
\`\`\`
```

**然后**：在对话框里发这条提示词，开始第一个任务：

```
请先读取以下文档建立上下文：
1. docs/项目背景.md
2. docs/编码规范.md
3. docs/开发进度.md

读完后，请开始执行任务 T001：项目初始化。

【验收标准】
- 初始化 Vite + React + TypeScript 项目
- 配置 Tailwind CSS
- 建立标准目录结构
- 能正常启动开发服务器

【约束】
- 严格遵循 编码规范.md
- 完成后更新 docs/开发进度.md，把 T001 标记为 ✅已完成
```

**看什么**：AI 会执行初始化命令、创建文件。完成后它应该自动更新 `开发进度.md`。如果没有自动更新，提醒它："请更新 开发进度.md"。

---

### 第 4 步：继续下一个任务

**操作**：在同一个对话里，继续发：

```
T001 已完成。请继续执行 T002：类型定义。

请先读取 docs/编码规范.md 确认风格，然后：
1. 在 src/types/ 下定义 Todo 相关类型
2. 包含：Todo 接口、FilterType 联合类型
3. 完成后更新 开发进度.md
```

**重复这个过程**，直到所有任务完成或你需要休息。

---

### 第 5 步：关掉对话，下次继续（关键操作）

**场景**：今天做到一半要下班了，关掉 Trae。明天重新打开，新开一个对话。

**错误做法**：直接说"继续昨天的开发"。AI 不知道"昨天"是什么。

**正确做法**：新对话的第一条消息，复制粘贴这个模板：

```
你好，请先读取以下三个文档建立项目上下文：
1. docs/项目背景.md
2. docs/编码规范.md
3. docs/开发进度.md

读完后用 3 句话总结：
- 项目在做什么
- 当前开发到哪一步
- 接下来要做什么

确认无误后等我下指令。

【约束】
- 所有代码遵循 编码规范.md
- 完成任务后必须更新 开发进度.md
```

**看什么**：AI 会读取三个文件，然后告诉你项目状态。你确认它说的对，再给它下一个任务。

**然后继续执行任务**（用第 4 步的模板，换个任务名即可）。

---

### 第 6 步：任务没做完就要中断时

**场景**：正在做 T003 列表页，做到一半要停。

**操作**：停之前，在对话里发：

```
我现在要中断 T003 的开发。请帮我更新 docs/开发进度.md，记录：

1. T003 状态改为"🔄进行中"
2. 进度改为 60%
3. 在"上次中断位置"部分写清楚：
   - 已完成：xxx
   - 未完成：xxx
   - 最后修改的文件：xxx
   - 下次该从哪继续：xxx
```

**下次恢复时**，用第 5 步的模板开新对话，AI 读完文档就知道从哪接上。

---

### 三句话总结这套用法

1. **建三个固定文档**（背景/规范/进度），作为 AI 的长期记忆。
2. **每次新对话开头**，先让 AI 读这三个文档再下任务。
3. **每次任务结束**，强制让 AI 更新进度文档。

就这三条，反复用。

---

## 1. 核心心法：结构化提示词四要素

无论生成什么文档，每条提示词都应尽量包含以下四块内容。缺一块，AI 的输出质量就会下降一档。

| 要素 | 作用 | 示例 |
|------|------|------|
| **角色（Role）** | 设定 AI 的身份与视角，决定输出的专业度与语气 | "你是一名有 10 年经验的资深后端架构师" |
| **上下文（Context）** | 提供背景信息、已有产物、技术栈、约束条件 | "项目使用 NestJS + TypeORM + PostgreSQL，已存在 User 模块" |
| **任务（Task）** | 明确要做什么、产出什么、格式是什么 | "生成一份 PRD，包含背景/目标/功能列表/非功能需求/验收标准" |
| **约束（Constraints）** | 限定边界、风格、字数、禁止项 | "不要写代码实现；功能列表用表格；每条不超过 30 字" |

**记忆口诀**：角色定调、上下文定界、任务定形、约束定质。

---

## 2. 用法一：生成需求文档

### 2.1 场景
项目刚启动，或新功能立项时，需要一份清晰的需求文档（PRD）作为后续开发、测试、沟通的依据。

### 2.2 提示词模板

```
你是一名资深产品经理，擅长把模糊想法拆解为可执行的需求文档。

【项目背景】
- 项目名：xxx（一句话描述项目定位）
- 技术栈：xxx
- 当前阶段：立项 / 迭代 / 重构
- 已有相关模块：xxx（如有则列出，没有写"无"）

【目标用户与痛点】
- 目标用户：xxx
- 核心痛点：xxx

【本次需求目标】
一句话说清这次要解决什么问题。

【产出要求】
请生成一份 PRD，包含以下章节，用 Markdown 输出：
1. 背景与目标
2. 名词解释（如有领域术语）
3. 功能列表（用表格：功能点 / 优先级 P0-P2 / 描述 / 验收标准）
4. 用户流程（用文字或 Mermaid 流程图）
5. 非功能需求（性能 / 安全 / 兼容性）
6. 未纳入本次范围的事项（明确边界）

【约束】
- 不要写代码实现
- 功能点总数不超过 10 个
- 每个功能点描述不超过 50 字
- 如信息不足，先列出需要我澄清的问题，再生成草稿
```

### 2.3 关键技巧

- **先问后答**：在模板末尾加"如信息不足，先列出需要澄清的问题"，可避免 AI 凭空脑补需求。**进阶**：复杂项目可升级为硬约束——"必须先列出需要我澄清的问题，等我回复后再产出 PRD；在此之前不要直接生成 PRD 正文"，比软提示更稳。
- **用表格约束粒度**：表格天然限制每条的字数，防止 AI 写成长段落。
- **明确"不做什么"**：第 6 章"未纳入范围"是防止需求蔓延的关键，比"要做什么"更重要。
- **要求 Mermaid 图**：流程图比文字更易评审，AI 生成 Mermaid 语法准确率较高。
- **非功能需求给维度清单**：不要只写"包含非功能需求"，应明确列出维度（性能 / 安全 / 可用性 / 可维护性 / 兼容性），否则 AI 容易漏项。
- **角色/范围硬约束防自发挥**：AI 容易自行扩范围（如 PRD 没说"超级管理员"，AI 自己加了第 5 类角色）。对枚举类边界要写硬约束："仅以下 4 类，不得自行扩展第 5 类"。
- **跨优先级禁止**：禁止"同一功能点夹带其他优先级项"（如 P0 功能里写"P1 增强"），应拆为独立功能点归入对应优先级。

### 2.4 进阶：迭代式需求收敛

第一次生成的 PRD 通常不完美，用"三轮收敛法"打磨：

```
第一轮：生成草稿（用上面的模板）
第二轮：扮演评审者，找出 PRD 中 3 个最可能出问题的点
第三轮：针对每个问题，给出修改建议并输出最终版
```

### 2.5 复杂场景增强模板（权限/多角色/多对多项目）

涉及权限模型、多对多关系、复杂角色边界时，基础模板不够用，需在【产出要求】和【约束】中追加：

```text
【产出要求】追加章节：
7. 附录 A：权限矩阵速查表（操作 × 角色，角色列固定为 X 类：xxx，不得增加其他角色列）
8. 附录 B：边界与异常分支要点（汇总多对多场景、退组/解散、越权拦截、删除分级等边界）

【约束】追加：
- 鉴权模式统一（如统一 JWT，不要并列 Cookie/CSRF 模式描述）
- 同一功能点不得跨优先级（如 P0 功能中夹带"P1 增强"项，应拆为独立功能点归入 P1）
- "操作权限边界拦截"作为统一规则写入附录 B，不再单独列为功能点，避免与其他功能点验收标准重叠
- 针对权限模型、组关系等多对多场景，需在功能描述中说明边界条件与异常分支
- 验收标准需包含：前置条件 / 操作步骤 / 预期结果 三要素
- 直接写入文件路径：xxx/项目背景.md（省去手动保存）
```

> 实战要点：复杂 PRD 一定要附录 A（权限矩阵）+ 附录 B（边界汇总），让 AI 把散落在 28 个功能点里的边界规则集中到附录，便于评审与回归。

---

## 3. 用法二：生成开发计划

### 3.1 场景
需求确定后，需要拆解为可执行的开发任务，明确顺序、依赖、工作量。

### 3.2 提示词模板

```
你是一名技术负责人，擅长把需求拆解为可执行的开发计划。

【输入文档】
- 需求文档：见附件 / 见上方 PRD
- 技术栈：xxx
- 团队规模：x 人
- 工期约束：x 周内完成

【产出要求】
请生成一份开发计划，用 Markdown 输出，包含：

1. 技术方案概要
   - 架构选型与理由
   - 模块划分（用 Mermaid 架构图）
   - 关键技术风险

2. 任务拆解（WBS）
   用表格：
   | 任务ID | 任务名 | 模块 | 依赖任务 | 预估工作量(人日) | 优先级 | 验收标准 |

3. 里程碑
   用表格：
   | 里程碑 | 日期 | 交付物 | 验收人 |

4. 开发顺序建议
   按依赖关系排序，标注哪些可并行。

【约束】
- 任务粒度：单个任务工作量不超过 2 人日，超过则继续拆
- 不要包含具体代码
- 依赖关系必须明确，不能出现循环依赖
- 如有歧义，先提问再生成
```

### 3.3 关键技巧

- **任务粒度控制**：单个任务不超过 2 人日，这是可跟踪的最小单位。超过 2 人日的任务往往是"假任务"，实际执行时会失控。
- **依赖关系显式化**：用"依赖任务"列，而不是靠顺序暗示。AI 生成的依赖关系需要人工复核。
- **验收标准必填**：没有验收标准的任务 = 无法判断是否完成。
- **并行机会标注**：好的开发计划要回答"哪些可以并行做"，而不只是"按顺序做"。
- **WBS 字段扩展**：复杂项目 WBS 至少 9-11 字段（任务ID / 任务名 / 模块 / **阶段** / **关联功能点** / 依赖任务 / 预估 / 优先级 / **负责人角色** / **产出物** / 验收标准）。其中"关联功能点"列指向 PRD 的 F01-Fxx，是上下游可追溯性的关键。
- **必含非功能任务**：WBS 不能只拆功能开发，至少显式包含 7 类：环境搭建 / 数据建模 / 权限引擎 / 接口契约与 Mock / 测试（含越权测试）/ 联调回归 / 部署上线。
- **P0 功能 100% 覆盖**：约束中明确"P0 功能点（F01-Fxx）必须 100% 覆盖到 WBS 任务，不得遗漏"，并显式列出 P0 范围（如 F01-F16），防止 AI 用旧的范围口径。
- **上下游矛盾以上游为准**：开发计划提示词中加一句"与 PRD 矛盾的措辞一律以 PRD 为准（如角色数量、P0 范围、鉴权模式）"，避免 AI 用记忆中的旧 PRD 口径。

### 3.4 与需求文档的衔接

开发计划必须基于 PRD 生成，不要让 AI 凭空拆任务。提示词中务必引用上一份 PRD 的内容或文件路径。

### 3.5 复杂场景增强模板（权限引擎/单人+AI 辅助项目）

涉及复杂权限模型、单人 + AI 辅助开发时，基础模板不够用，需追加：

```text
【执行前置】
- 必须先用 Read 工具读取 PRD 全文，不得凭上下文臆测
- 若发现关键信息（团队规模 / 交付范围 / 工期 / 数据模型字段 / 接口契约）在 PRD 中缺失或矛盾，
  必须先列出澄清问题等我回复，再产出开发计划；在此之前不要直接生成开发计划正文

【产出要求】追加：
1. 技术方案概要中加：权限引擎方案（与 PRD 附录 A 对齐）、删除分级实现方案、接口契约约定
2. WBS 表格字段扩展为 11 列（见 3.3 关键技巧）
3. WBS 必含 7 类非功能任务（见 3.3 关键技巧）
4. 开发顺序建议：
   - 用 Mermaid gantt 图给出按工作日的单人串行排期（按天粒度，标注周末）
   - 用 Mermaid flowchart 画任务依赖关系图，标注关键路径（critical path）
   - 标注里程碑节点（M1 工程骨架 / M2 权限引擎 / M3 联调 / M4 验收+越权测试 / M5 P1 增强 / M6 部署）

【约束】追加：
- P0 功能点（F01-Fxx）必须 100% 覆盖到 WBS 任务，不得遗漏
- 权限相关任务必须包含越权测试用例为验收标准之一
- 单个任务不超过 2 人日；超 2 人日的任务必须拆分
- 与 PRD 矛盾的措辞一律以 PRD 为准（角色数量、P0 范围、鉴权模式等）
- 单人 + AI 辅助场景下，"并行"指 Trae 辅助产出非关键路径产物（Mock、测试用例），
  明确哪些可借助 AI 并行产出素材、哪些必须人工串行验收
```

> 实战要点：①"执行前置"段强制 AI 先 Read PRD 再拆任务，避免凭记忆臆测；②单人场景下"并行"语义要说清，不能让 AI 假设有多个开发者；③越权测试用例必须写进 WBS 验收标准，不能只靠开发自觉。

---

## 4. 用法三：生成开发进度

### 4.1 场景
开发过程中，需要定期生成进度报告，用于团队同步、风险预警、向上汇报。

### 4.2 提示词模板

```
你是一名项目经理，擅长生成清晰、可决策的进度报告。

【输入信息】
- 开发计划：见上方 WBS
- 本周期：2026-07-10 至 2026-07-17
- 已完成任务：T001、T002（已完成代码，待联调）
- 进行中任务：T003（前端列表页，进度 60%）
- 阻塞任务：T005（依赖第三方接口，对方未交付）
- 新增变更：需求方追加一个导出功能

【产出要求】
生成一份周报，用 Markdown 输出，包含：

1. 总体进度
   - 计划完成率：x%
   - 实际完成率：x%
   - 健康度：🟢正常 / 🟡风险 / 🔴延期

2. 本周完成（表格：任务ID / 名称 / 状态 / 产出物）

3. 进行中（表格：任务ID / 名称 / 进度% / 预计完成 / 风险）

4. 阻塞与风险
   - 每条包含：阻塞点 / 影响 / 需要谁协助 / 截止时间

5. 下周计划
   - 列出下周要启动的任务

6. 变更记录
   - 本周新增/变更的需求及其影响

【约束】
- 不要空话套话（"本周进展顺利"这类废话删除）
- 风险必须可执行：要有具体动作和负责人
- 数字必须真实，不要编造
```

### 4.3 关键技巧

- **健康度三色灯**：强制 AI 给一个总体判断，逼迫它综合评估，而不是罗列完事。
- **风险必须可执行**：每个风险点都要回答"需要谁、做什么、什么时候"，否则就是无效风险。
- **禁止套话**：明确写"不要空话套话"，AI 容易写出"本周进展顺利，下周继续努力"这种废话。
- **变更记录独立成章**：需求变更是项目失控的主要源头，必须单独追踪。

### 4.4 进度数据从哪来

进度报告的输入数据需要真实，来源：
- Trae 中已完成的任务记录
- Git 提交记录（可让 Trae 读取 `git log` 总结）
- 手动输入的进行中任务状态

可让 Trae 自动读取 git log 生成进度草稿：

```
读取最近一周的 git log，按功能模块归类，生成进度报告草稿。
未在提交记录中体现的任务（如设计、联调）需要我手动补充。
```

---

## 5. 用法四：跨会话延续编码（关键）

### 5.1 痛点
Trae 的对话是会话级的，新开一个对话后，AI 不知道：
- 之前做了什么
- 项目的技术约定、编码风格
- 当前进度卡在哪
- 还有哪些任务待做

如果每次都靠口头解释，既低效又容易遗漏。解决方案：**用项目级文档固化上下文，让 AI 每次都能"接上"**。

### 5.2 三件套：项目记忆文件

在每个项目根目录维护以下三个文件，让 AI 在新会话开始时优先读取：

| 文件 | 作用 | 更新频率 |
|------|------|----------|
| `docs/项目背景.md` | 项目背景、技术栈、架构、约定 | 项目级，少变 |
| `docs/编码规范.md` | 编码规范、命名风格、目录结构 | 项目级，少变 |
| `docs/开发进度.md` | 当前迭代进度、待办、阻塞 | 每次开发后更新 |

### 5.3 新会话启动提示词（最重要）

每次开启新对话或新任务时，第一条消息用这个模板：

```
你好，请先读取以下项目文档，建立项目上下文：

1. docs/项目背景.md  —— 项目背景与技术架构
2. docs/编码规范.md —— 编码规范与风格约定
3. docs/开发进度.md    —— 当前进度与待办任务

读完后，请用 3 句话总结：
- 这个项目在做什么
- 当前开发到哪一步
- 接下来要做什么

确认无误后，我再给你下一个具体任务。

【约束】
- 所有代码必须遵循 编码规范.md
- 完成任务后，必须更新 开发进度.md
- 如遇与文档冲突的设计决策，先向我确认，不要擅自决定
```

### 5.4 任务交接提示词模板

当上一个任务没做完，要新开对话继续时：

```
请继续开发任务 T003：用户列表页。

【上下文】
- 请先读取 docs/项目背景.md 和 docs/编码规范.md
- 进度见 docs/开发进度.md 中 T003 条目

【上次中断位置】
- 已完成：列表展示、分页
- 未完成：搜索功能、空状态
- 上次最后修改的文件：src/pages/UserList/index.tsx

【本次目标】
完成搜索功能（按用户名模糊搜索）和空状态组件。

【约束】
- 沿用现有目录结构和命名风格
- 搜索使用 debounce，300ms
- 空状态复用项目已有的 Empty 组件，不要新建
- 完成后更新 docs/开发进度.md
```

### 5.5 强制 AI 遵守编码风格的 5 个技巧

1. **规范文档化**：把编码风格写进 `编码规范.md`，而不是口头说。文档比记忆可靠。
2. **示例驱动**：规范文档里放"正确示例"和"错误示例"，AI 模仿示例的能力远强于理解抽象规则。
3. **每次任务前重申**：在任务提示词里加一句"请严格遵循 编码规范.md"，强化约束。
4. **完成后自检**：任务结束后让 AI 自查："请检查本次代码是否符合 编码规范.md，列出不符合的点并修正"。
5. **Git Hook 兜底**：用 lint + commit hook 做最后防线，AI 偶尔会违反规范，工具链是兜底。

### 5.6 编码规范.md 关键章节建议

```markdown
# 编码规范

## 技术栈
- 语言：TypeScript（strict mode）
- 框架：React 18 + 函数组件
- 状态：Zustand
- 样式：Tailwind CSS

## 命名约定
- 组件：PascalCase，如 UserProfile
- 函数：camelCase，如 fetchUserData
- 常量：UPPER_SNAKE_CASE，如 MAX_RETRY
- 文件：组件用 PascalCase，工具用 kebab-case

## 目录结构
src/
  components/    # 通用组件
  pages/         # 页面
  services/      # API 调用
  stores/        # 状态管理
  utils/         # 工具函数
  types/         # 类型定义

## 代码风格
- 禁止 any，必须显式类型
- 函数超过 50 行必须拆分
- 副作用集中在 useEffect，不要散落
- 导入顺序：react → 第三方 → 项目内 → 相对路径

## 正确示例
\`\`\`typescript
// ✅ 正确：函数组件 + 显式类型
interface UserListProps {
  users: User[];
  onEdit: (id: string) => void;
}

export function UserList({ users, onEdit }: UserListProps) {
  // ...
}
\`\`\`

## 错误示例
\`\`\`typescript
// ❌ 错误：使用 any
export function UserList(props: any) {
  // ...
}
\`\`\`
```

### 5.7 开发进度.md 模板

```markdown
# 开发进度

## 当前迭代：v1.2.0（目标日期：2026-07-30）

## 任务状态

| 任务ID | 任务名 | 状态 | 负责人 | 进度 | 备注 |
|--------|--------|------|--------|------|------|
| T001 | 用户登录 | ✅已完成 | - | 100% | 已上线 |
| T002 | 用户列表 | ✅已完成 | - | 100% | 待联调 |
| T003 | 用户详情 | 🔄进行中 | - | 60% | 搜索功能未完成 |
| T004 | 权限管理 | ⏳待开始 | - | 0% | 依赖T003 |

## 上次中断位置
- 任务：T003 用户详情
- 文件：src/pages/UserDetail/index.tsx
- 已完成：基础布局、数据展示
- 未完成：编辑功能、权限校验
- 下一步：实现编辑表单，复用 UserForm 组件

## 已知问题
- API /user/detail 返回字段与类型定义不一致，待后端修复

## 变更记录
- 2026-07-15：新增导出功能需求，加入 T005
```

---

## 6. 项目级配置文件模板

### 6.1 最小可用结构

```
项目根目录/
├── docs/
│   ├── 项目背景.md     # 项目背景
│   ├── 编码规范.md    # 编码规范
│   └── 开发进度.md        # 开发进度
├── .trae/
│   └── rules.md                # Trae 工具约定路径，保留英文名以被识别
└── src/
```

### 6.2 项目背景.md 模板

```markdown
# 项目背景

## 一句话定位
本项目是一个 xxx，解决 xxx 问题。

## 技术架构
- 前端：React 18 + TypeScript + Vite
- 后端：NestJS + TypeORM
- 数据库：PostgreSQL
- 部署：Docker + Nginx

## 核心模块
1. 用户模块：注册、登录、权限
2. 订单模块：下单、支付、退款
3. ...

## 关键约束
- 必须支持 IE11（如有此类硬约束）
- 接口响应时间 < 200ms
- ...

## 外部依赖
- 支付：支付宝/微信
- 短信：阿里云短信
```

---

## 7. 常见反例与改进

### ❌ 反例 1：模糊指令
> 帮我写个用户管理功能。

**问题**：没有角色、没有上下文、没有产出格式、没有约束。
**改进**：用第 2 节模板，明确技术栈、产出格式、验收标准。

### ❌ 反例 2：一次说太多
> 帮我写需求文档、开发计划、测试用例，然后开始写代码。

**问题**：AI 一次性处理多任务，每项质量都会下降。
**改进**：拆成多次对话，每次只产出一类文档，前一份作为后一份的输入。

### ❌ 反例 3：新对话不提供上下文
>（新对话）继续写昨天那个功能。

**问题**：AI 不知道"昨天那个功能"是什么。
**改进**：用第 5.4 节的任务交接模板，显式提供中断位置和文件路径。

### ❌ 反例 4：只下达令，不建规范
> 按我的风格写代码。

**问题**：AI 无法推断"你的风格"是什么。
**改进**：把风格写进 `编码规范.md`，每次引用。

### ❌ 反例 5：完成后不更新进度
>（任务做完后直接结束对话）

**问题**：下次新对话时，进度文档还是旧的，AI 接不上。
**改进**：在任务提示词末尾强制要求"完成后更新 开发进度.md"。

### ❌ 反例 6：AI 自行扩展范围
> PRD 只定义了 4 类角色，生成出来的 PRD 附录里却多了一列"超级管理员"。

**问题**：AI 倾向于"补全"它认为合理的枚举（角色、状态、权限），导致范围蔓延且与上游矛盾。
**改进**：对枚举类边界写硬约束——"仅以下 4 类，不得自行扩展第 5 类角色"。复杂业务一定要附录 A（权限矩阵）+ 附录 B（边界汇总）集中呈现，便于评审发现 AI 的自发挥。

### ❌ 反例 7：上下游文档措辞冲突
> PRD 已改为 4 类角色，但开发计划提示词还在写"5 级权限判定"。

**问题**：上游 PRD 更新后，下游提示词（开发计划/进度/编码）未同步，AI 按旧口径产出，导致整条链路返工。
**改进**：①下游提示词加"执行前置：必须先 Read PRD 全文，不得凭上下文臆测"；②加约束"与 PRD 矛盾的措辞一律以 PRD 为准"；③涉及 P0 范围、角色数量、鉴权模式等关键口径，在提示词中显式重述当前值（如"P0 功能点 F01-F16"），而非引用旧编号。

### ❌ 反例 8：验收标准与全局规则重复
> 每个功能点的验收标准都重复写一遍"越权返回 403"，28 个功能点重复 28 次。

**问题**：规则散落，修改一处需改 28 处，且 AI 容易写出互相矛盾的版本（如有的写 403、有的写 409）。
**改进**：把全局规则（越权拦截、删除分级、幂等处理）集中到附录 B，各功能点验收标准不再重复，只写"越权拦截规则见附录 B"。

---

## 附：提示词检查清单

每次写提示词前，对照这 8 个问题：

**基础四要素**
- [ ] 是否设定了角色？
- [ ] 是否提供了上下文（技术栈、已有产物）？
- [ ] 是否明确了产出格式（章节、表格、图）？
- [ ] 是否写了约束（字数、禁止项、边界）？

**信息流转**
- [ ] 是否要求"信息不足时先提问"？复杂场景是否升级为硬约束（"先问，否则不要产出正文"）？
- [ ] 下游提示词是否要求"先 Read 上游文档全文，再产出"？是否加了"与上游矛盾处以上游为准"？

**复杂业务专项**
- [ ] 枚举类边界（角色/状态/权限）是否写了硬约束（"仅 X 类，不得扩展"）？
- [ ] 全局规则是否集中到附录，而非散落在各功能点重复？

8 个都打勾，输出质量通常不会差。
